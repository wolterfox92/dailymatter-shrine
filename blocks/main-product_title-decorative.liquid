{% liquid
  if section.settings.product_images == 'all' or section.settings.product_images == 'one'
    assign product = section.settings.product
  endif
%}
<div
  class="product__title product__title-decorative-{{ block.id }} {{ block.settings.visibility }}"
  {{ block.shopify_attributes }}
  style="margin-top: {{ block.settings.margin_top | divided_by: 10.0 }}rem;margin-bottom: {{ block.settings.margin_bottom | divided_by: 10.0 }}rem;"
  aria-hidden="true"
>
  <p class="h1 product__title-decorative{% if block.settings.uppercase_title %} product-title--uppercase{% endif %} {{ block.settings.alignment }} {{ block.settings.mobile_alignment }}">
    {{ product.title }}
  </p>
  <style>
    .product__title-decorative-{{ block.id }} .product__title-decorative {
      font-size: {{ block.settings.desktop_size | divided_by: 1.3 | times: settings.heading_scale  | divided_by: 1000.0  }}rem;
      margin: 0;
    }
    @media screen and (max-width: 749px) {
      .product__title-decorative-{{ block.id }} .product__title-decorative {
        font-size: {{ block.settings.mobile_size | divided_by: 1.3 | times: settings.heading_scale  | divided_by: 1000.0 }}rem;
      }
    }
  </style>
</div>

{% schema %}
{
  "name": "Product title",
  "tag": null,
  "settings": [
    {
      "type": "select",
      "id": "visibility",
      "options": [
        {
          "value": "desktop-hidden",
          "label": "Mobile only"
        },
        {
          "value": "mobile-hidden",
          "label": "Desktop only"
        },
        {
          "value": "always-display",
          "label": "All devices"
        }
      ],
      "label": "Display on",
      "default": "always-display"
    },
    {
      "type": "checkbox",
      "id": "uppercase_title",
      "label": "Uppercase title",
      "default": false
    },
    {
      "type": "header",
      "content": "Desktop"
    },
    {
      "type": "range",
      "id": "desktop_size",
      "min": 24,
      "max": 60,
      "step": 3,
      "unit": "px",
      "default": 39,
      "label": "Size"
    },
    {
      "type": "select",
      "id": "alignment",
      "options": [
        {
          "value": "left",
          "label": "Left"
        },
        {
          "value": "center",
          "label": "Center"
        },
        {
          "value": "right",
          "label": "Right"
        }
      ],
      "default": "left",
      "label": "Alignment"
    },
    {
      "type": "header",
      "content": "Mobile"
    },
    {
      "type": "range",
      "id": "mobile_size",
      "min": 20,
      "max": 50,
      "step": 2,
      "unit": "px",
      "default": 28,
      "label": "Size"
    },
    {
      "type": "select",
      "id": "mobile_alignment",
      "options": [
        {
          "value": "mobile-left",
          "label": "Left"
        },
        {
          "value": "mobile-center",
          "label": "Center"
        },
        {
          "value": "mobile-right",
          "label": "Right"
        }
      ],
      "default": "mobile-left",
      "label": "Alignment"
    },
    {
      "type": "header",
      "content": "Margins"
    },
    {
      "type": "range",
      "id": "margin_top",
      "label": "Top",
      "min": 0,
      "max": 45,
      "step": 3,
      "unit": "px",
      "default": 15
    },
    {
      "type": "range",
      "id": "margin_bottom",
      "label": "Bottom",
      "min": 0,
      "max": 45,
      "step": 3,
      "unit": "px",
      "default": 15
    }
  ],
  "presets": [
    {
      "name": "Product title"
    }
  ]
}
{% endschema %}
